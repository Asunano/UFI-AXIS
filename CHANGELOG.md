# 更新日志

## v0.0.3 (2026-09-26)

> App、Core、Web 三端同步发布。Web 管理后台随 Core 端一同更新，故其更新内容并入下方「Core 端」。

### ⚠️ 重要变更

- **无电池机型不再显示假电量**：部分机型恒报 50%，此前会被当成真实电量入库并触发低电量告警；现在这类设备不记录、不告警，电池详情页直接说明本机无电池
- **底部导航改版**：改为贴底通栏 Dock，监控从二级页提升为常驻主入口（仪表盘 / 网络 / 监控 / 工具 / 我的）
- **WiFi 频段切换改走独立接口**：修正了「开启 WiFi 会把设备频段悄悄切回 2.4G」的问题，切换前会提示可能掉线
- **日志开关口径变更**：WARN / ERROR 级别不再受日志总开关影响，始终落盘以保留故障现场；关闭总开关会同时关闭详细级别
- **更新镜像不再手动选择**：原「镜像源 / 自定义镜像地址」设置项移除，改由设备判断出网地区后统一下发；升级后原先填写的自定义镜像地址不再生效

---

### 📱 App 端（Android）

#### ✨ 新功能

- **媒体中心**：音乐 / 视频 / 图片三个独立媒体库，可按名称、日期、大小排序并在列表与网格间切换；数据来自设备系统媒体库，不必逐层翻文件夹
- **音乐播放器**：支持锁屏、通知栏与耳机按键控制，退出界面后继续播放；歌名 / 歌手 / 专辑改为直接解析文件标签（FLAC 的 Vorbis Comment、MP3 的 ID3，含 UTF-8 / GBK / Latin-1 编码嗅探），不再只显示文件名
- **播放队列带范围概念**：从专辑 / 歌手 / 文件夹 / 歌单进入点歌后，下一首与随机播放只在该范围内走，不会跳到整库的其它歌；循环模式、随机开关与队列顺序（含手动插歌、移除、拖拽）都会记住，播放服务被系统杀掉或重启 App 后可恢复
- **歌词与歌单**：歌词取自文件内嵌标签或同名歌词文件；支持建立与播放歌单
- **迷你播放条与正在播放胶囊**：离开播放页后仍可在列表页与标题栏看到当前曲目并直接控制播放
- **视频播放器**：支持内嵌与外挂字幕（.srt / .ass）选择、全屏与窗口两种模式，播放中保持亮屏，播放失败会给出提示而不是黑屏
- **媒体设置页**：音频与视频各一套，含扫描范围、缩略图缓存统计与清理、封面批量生成
- **音乐库移除名单**：长按歌曲可「从音乐库移除」，只改名单不动文件，可在专门页面随时撤销，接近上限时提前提示
- **视频缩略图兜底**：设备解不出画面时改由手机本地抽帧生成并回传，之后两端都能看到封面；失败不打断浏览，6 小时内不重复尝试同一项
- **媒体下载队列与历史**：可查看在队 / 已完成 / 失败的条目与失败原因，支持清空
- **媒体扫描范围设置**：可指定或排除参与扫描的目录，并可请求设备重新收录刚推送进去的文件
- **流量历史页**：按日 / 周 / 月 / 年查看用量柱状图；未到的时间画空柱，不会与「那天没用流量」混淆
- **标题栏挂件**：新增天气与每日一言（诗词），无需自备 API key；位置属设备级配置，App 与 Web 共享同一份设置
- **在线设备弹窗**：仪表盘「已连接设备」卡可直接查看设备名 / 接入方式 / IP / MAC 并拉黑；LAN 接入的设备标注 LAN，不再给出无效的拉黑按钮
- **流量限额弹窗**：仪表盘「本月流量」卡可直接设置限额值与单位、告警阈值、自动清零、到达阈值关网，不必绕到工具页
- **启动页**：冷启动显示「正在连接设备」与分步进度；连不上时在同一页给出原因、下一步指引与动作按钮，而不是空白等待
- **界面附加项设置**：新增独立设置页，集中开关标题栏挂件一类的展示项

#### 🔧 优化

- **更新来源改由设备统一决定**：是否走镜像、走哪个镜像由设备判断出网地区后下发，海外设备不会再被强行推到国内镜像；设备连不上时沿用本地缓存的模式，仍可自行更新
- **深色模式配色重做**：卡片与页面底的明度差下限提高、分隔线透明度从 15% 降到 6%、Hero 渐变改为往白提亮，解决「玫红等配色下卡片浮不起来、分隔线比卡片还抢眼」的观感问题
- **监控图表断档判定**：改为按局部采样节奏逐个间隔判断，采集间隔在 3s 与 60s 之间跳变时不再把稀疏区切成满屏碎段，真正的长时间空洞仍然断开
- **下载设置改为确认后再提示**：四个子页不再「一点保存就显示已保存」，改为等设备确认再提示，并写明保存的是哪一项；失败以红色提示呈现而不是静默
- **播放进度条手感**：换歌时进度平滑过渡而不是瞬跳回弹；拖动中途换歌不再卡在「一直按着」的状态导致进度与时间停更
- **更新弹窗终态动画**：完成与失败不再只有一行文字，进度环会收口成整圆并描出对勾或叉；系统开启「移除动画」时直接显示终态
- **调试入口可关闭**：关于页的调试组新增「调试模式」开关，关掉即收起入口，不必清应用数据；连点版本号 5 次可重新解锁
- **播放进度与最近播放记录读写串行化**：修掉并发下「已清空的条目被后台写回复活」与残留键持续堆积的问题

#### 🐛 修复

- **二级页面底部一条带子上的控件点不动**：底栏隐藏期间仍占据触摸区域，音乐播放页的进度条与播放键正好整排落在其中，表现为点不动或点偏才响应
- **部分 Android 12~14 机型底部出现白带**
- **改 SSID 或不带密码提交配置会把 WiFi 口令写坏甚至清空**：现在缺省密码沿用设备当前值
- **中文 SSID 与短信内容乱码**：设备返回文本改为 UTF-8 优先、GBK 回落

---

### 🖥 Core 端（管理后台，含 Web 前端）

> Web 管理后台随 Core 一同发布，以下含两端更新。

#### ✨ 新功能

- **远端存储源**：文件管理支持接入 FTP / WebDAV / SMB / S3 四类远端存储，可增删改与连接测试（带延迟显示），密码返回时脱敏
- **分片上传与断点续传（Web）**：上传带进度、可取消，单片失败或网络抖动后从已传部分接着传；刷新页面后重新选择同一文件即可续上；是否分片由设备下发的能力位决定
- **远端推送作业**：上传到远端存储源时，文件先落到设备暂存目录，再作为一条可追踪的作业推往远端，重复提交同一文件不会被误报成上传成功
- **媒体接口族**：媒体列表、权限状态（视频 / 音乐 / 图片分别回报）、重新扫描、音频标签、歌词、字幕、音频分组聚合
- **在线播放票据**：浏览器里的音视频可直接边下边播并支持拖动进度，不再需要先把整个文件下成 blob（此前有 100MB 上限，电影根本进不了预览）；票据滑动过期 10 分钟，且只授权当前播放的那一个文件
- **媒体页（Web）**：音乐 / 视频 / 图片三个视图与音频播放器面板
- **文件页搜索与预览扩充（Web）**：新增搜索弹窗、文件类型图标，预览支持的格式大幅增加
- **流量历史（Web）**：新增历史弹窗与图表，口径与 App 端一致；用量按小时入库、按日 / 周 / 月 / 年出桶，未到的时间也返回 0 值桶以保证横轴完整
- **标题栏挂件（Web）**：天气与每日一言，并补齐移动端适配判定
- **出网地区判定**：设备统一判断出网国家，据此决定更新下载走直连还是镜像
- **排障与诊断增强**：新增字段归一化、裸命令通道等排障开关（即时生效）；诊断页新增字段覆盖率视图（支持分组摘要与复制 JSON）、设备插件标识与选型依据

#### 🔧 优化

- **设备适配分层**：设备协议实现下沉为可插拔插件，新增运行环境探测参与机型选型，为后续适配其它品牌机型留出接口；对现有机型行为不变
- **远端存储源连接测试加超时上限**：按配置超时 + 5s、硬顶 30s，超时文案写明等待了多少秒；反复点测试不会再占满设备 IO 线程池导致其它文件操作一起卡住
- **温度读取移出请求线程**：`/api/device/thermal` 需要读十几次同步文件，此前会阻塞同一批请求
- **AT 指令通道退避**：模块无响应时按 500ms 起翻倍、上限 30s 主动降频，不再每次调用都硬等到超时，设备整体响应更快
- **WiFi 配置补齐**：加密方式与最大接入数（上限 10）可在 App 与 Web 直接设置
- **远端文件在线预览取不到大小时退回整量返回**：功能降级但不报错；客户端主动中断不再被计为一次失败

#### 🔒 安全加固

- **WebDAV 跳过证书校验时仍校验主机名**，避免账号口令被中间人获取
- **媒体目录白名单改为分隔符敏感 + 真实路径判定**，`/sdcardXYZ` 这类同前缀旁路目录与指向白名单外的符号链接会被正确拒绝
- **安装器加固**：包名与远端文件名做白名单校验，避免特殊字符被设备侧 shell 解释执行；Linux 一键脚本的临时文件改用 `mktemp`，防止 `/tmp` 同名符号链接劫持

#### 🐛 修复

- **温控保护不生效**：调度器温度读取失效导致高温时不降速
- **信号页 SNR 读不到**：回退分支使用了错误的字段名
- **在线设备与事件统计偶发为空**：设备两种响应形态只兼容了其中一种
- **某组 WiFi 设置读不回**：命令登记误用了响应字段名

---

### 🧰 安装与部署

- **安装器新增远程 ADB 无线安装**：重写地址解析与权限授予流程，支持 IPv6 字面量地址；安装流程的等待确认环节不再可能卡死

---

## v0.0.2 (2026-09-12)

> App、Core、Web 三端同步发布。Web 管理后台随 Core 端一同更新，故其更新内容并入下方「Core 端」。

### ⚠️ 重要变更

- **通知开关重新梳理**：此前一个「告警通知」开关同时兼任全局总闸、告警分类与隧道分类 —— 关掉它会把短信通知与后台守护一并静默停掉，界面文案也与实际不符。现在拆分为总闸与各分类独立开关，升级后请重新确认各分类的开关状态
- **新增「本机短信回发」渠道**：通知可经设备自己的 SIM 卡发成短信。该渠道走信令网、**按条计费**，默认每日上限 5 条，可在渠道页调整或关闭
- **数据库自动升级**：设备端数据库由 v8 升至 v13，开机后自动迁移，历史告警与配置保留
- **隧道提醒独立**：隧道通知不再借用告警开关，可单独关闭而保留阈值告警

---

### 📱 App 端（Android）

#### ✨ 新功能

- **通知渠道体系**：邮件、Webhook、本机短信三条渠道统一为同一套规则 —— 最低级别、每日上限、适用场景，任一渠道页都是这三个旋钮，可分别配置与测试发送
- **Webhook 通知**：内置 7 套预设，填入地址即可用 —— 自定义、Bark、ntfy、企业微信群机器人、飞书群机器人、PushPlus、Server 酱；模板占位符可直接引用告警级别、场景、时间等字段
- **本机短信回发**：经设备 SIM 卡把通知发成短信，单条上限 70 字（超长自动裁剪，不会截断半个表情）；带号码校验与配额提示
- **推送渠道总览**：新增渠道总览页，一屏看清三条渠道各自「配没配、开没开」与今日已发量（如 2/5），并直接说明某条渠道**为什么现在不生效**（总开关关闭 / 配置未填完 / 场景未勾选）
- **通知管理页**：分类开关、免打扰时段、测试发送、设备告警、系统通知记录入口集中在一页
- **通知记录可追溯**：新增「系统通知历史」，每条状态栏通知的结果都留档，包括**被拦下的那些与拦下的具体原因**（总闸关、分类关、系统未授权、重复、免打扰等），不再只有「没收到」三个字
- **投递记录**：三条渠道共用一页，按「已发出 / 没发出 / 已跳过」三态留档；**失败数与跳过数分开统计**，跳过项带中文原因说明
- **短信拦截**：新增拦截规则，可按**发件号码**或**短信正文**（或两者同时）匹配，支持包含 / 完全相等 / 前缀 / 后缀四种方式；另设拦截记录页，规则带命中次数统计，可在记录里看到每一条被拦短信命中了哪条规则
- **短信设置页**：短信功能集中成独立页面，含拦截总开关等
- **备份与恢复**：设备配置可加密导出为文件（口令派生密钥 + AES-256-GCM 加密）；导入前可先预览变更清单，确认后再落盘
- **控制台命令历史**：终端页记录历史命令，可回看与复用；历史由 Core 统一保存，App 与 Web 看到的是同一份
- **文本编辑器**：文本类文件支持查找替换，新增工具栏与状态栏；可手动选择编码（GBK 等）打开，避免中文乱码
- **统一文件预览**：图片、音频、视频、文本并入同一个预览浮层，含自定义播放控制器；原独立的图片查看器与媒体页已移除
- **后端掉线提示**：服务不可达时主动弹窗，提供「重试」与「服务器设置」两个入口，而不是笼统的「加载失败」
- **验证码自动复制**：新收到的验证码短信会自动识别并集中展示，点一下即可复制（需先在系统中开启本应用的无障碍服务，否则不生效）
- **验证码豁免拦截**：可让验证码短信不受关键词拦截规则影响，避免验证码被误拦（仍受号码黑名单约束）
- **监控数据导出到手机**：监控数据可导出为 CSV 保存到手机（按日期分目录），8 类指标各一份，可一并打包为 ZIP
- **应用隐身**：可把本应用从系统「最近任务 / 概览」列表隐藏，降低被误关的概率、保护隐私（与后台常驻配合更稳妥）
- **修改配对密码**：可直接在应用内修改设备配对密码（旧密码即管理权限证明），与日常设置保存相互独立
- **限制可配对设备数量**：可开启配对数量上限，避免设备被无限配对
- **设备离开提醒**：新增「设备离开」告警场景，与已有的设备接入提醒并列

#### 🔧 优化

- **监控总览按告警类型聚合**：同类告警合并为一行并可展开明细，告警爆发时不再刷屏
- **图表时间轴自适应**：轴标签格式随所选范围变化，不再出现「选了 7 天仍只显示时:分」
- **通知判定单点化**：场景判定、级别过滤、渠道选择统一由一处决定，三条渠道不再各自为政
- **免打扰口径统一**：支持跨天时段（如 23:00–07:00）；两个滑块拖到同一格视为「不静默」而非「整天静默」，并可按渠道单独选择是否受免打扰约束
- **页面转场与入场动画轻量化**：旧版高斯模糊入场在低端机掉帧，改为淡入 + 12dp 位移；页面切换动画同批调整
- **媒体播放数据源**：播放器升级至 media3 1.10.0，并改为复用带设备签名的网络栈，避免播放中途因请求被服务端拒绝而中断
- **网络制式切换状态**：切换期间在卡片上显示「切换中」标签，切完回读确认后再提示成功，不再出现「已点了切换、界面却仍显示旧档位」
- **新增公共组件**：环形占比图、可折叠分区、提示卡片
- **界面间距与动画统一**：同类元素的内外间距、动画时长收敛到同一套标准，卡片与横幅的默认样式集中定义
- **深色模式下发灰**：默认皮肤在深色模式下的强调色明暗方向取反，对卡片底色的对比度最低只有 1.9:1，表现为图标与文字发灰；已修正
- **日志目录分层**：日志按组件分目录（core / watchdog / keepalive 等），运行日志的保留策略不再把崩溃证据一起清掉

#### 🐛 修复

- **「通知和连接莫名失效且怎么也好不了」**：设备时钟漂移等可自愈的临时故障，此前会被误判为「凭据失效」并要求重新配对；现在只有确证吊销才清理凭据，可重试的失败交给客户端重试
- **一个开关关掉、其他功能被静默停掉**：修复隧道、短信、验证码、后台守护共用同一个开关键导致的连带失效，界面文案同步修正
- **免打扰把严重告警压成无声**：免打扰时段内原本所有通知一律静默，现在严重级别与断网类告警可见
- **告警记录条数不生效**：修复告警保留条数上限不生效、以及计数取值错误（把「非空探测」当计数用）导致数字不对的问题；原先写死且界面上看不见的保留规则一并去掉
- **写操作「第一次点必失败、再点一次就好」**：设备会话被顶掉或重注册后，写操作现在会重新登录并重试一次，不再把可自愈的抖动直接报成失败
- **邮件转发丢信**：修复「收到来信与本机发出的短信几乎同时落库」时来信被永久跳过、再也转发不出去的问题
- **崩溃日志污染目录**：崩溃记录不再落在品牌根目录下，与运行日志一并归档
- **套餐限额预警与设备接入 / 离开提醒此前不触发**：相关配置键缺失导致这几档告警始终不被触发，现已可正常开关与通知
- **超大文件编辑改为先下载到手机**：不再卡在页内预览，整份取回本地缓存后即可就地编辑

---

### 🖥 Core 端（管理后台，含 Web 前端）

> Web 管理后台随 Core 一同发布，以下含两端更新。

#### ✨ 新功能

- **通知分发中枢**：统一负责场景判定、级别过滤与多渠道路由；发送失败会自动重试一次，三条渠道各自独立计数与配额
- **服务端能力补齐**：Webhook 与本机短信渠道的配置读写与测试发送、设备配置的加密导出与预览导入、控制台命令历史的存取、短信拦截规则与拦截记录、Web 测速请求经设备转发 —— 均由服务端统一提供
- **短信拦截判定统一**：邮件转发、推送、验证码入库、短信列表、计数、联系人聚合六处共用同一份判据，定时轮询不会反复查库
- **验证码漏抓 / 误抓**：原先两条链路各有一份判据不同的实现（提示词与位数都不同），现合并为唯一一份，并限定匹配位置范围
- **「界面上关了但还在发」**：通知场景原先分散定义、存在拼写分叉；现统一由一处定义，邮件、推送、模板与 App 侧共用
- **写操作重试策略**：设备响应分为「成功 / 失败 / 会话失效可重试」三种，会话失效的重试最多两次，避免无界重试把后续读写一并阻塞
- **数据库自动升级（v8 → v13）**：新增短信拦截规则、拦截记录、控制台命令历史三张表；投递记录增加渠道字段，并把结果分为**已发出 / 没发出 / 已跳过**三态，使跳过与失败可分开统计
- **配对管理增强**：服务端支持修改配对密码（旧密码即管理权限证明，与设置保存分离）、限制最大可配对设备数量

- **通知渠道配置（Web 管理后台）**：Webhook 与本地短信渠道的配置弹窗（含 7 套预设一键填充）、渠道规则字段与投递记录查看
- **备份与恢复面板（Web 管理后台）**：导出、预览、导入；经内网穿透以 HTTP 访问时会主动提示链路上口令是明文
- **短信拦截规则（Web 管理后台）**：规则的新增 / 编辑 / 启停弹窗，含号码黑名单与正文关键词两类，可查看已拦截短信清单
- **音频标签与歌词（Web 管理后台）**：读取音频文件的 ID3 标签（标题 / 艺术家 / 专辑）与内嵌歌词并显示
- **终端命令历史（Web 管理后台）**：终端页可回看与复用历史命令
- **可选组件安装（Web 管理后台）**：frpc / cloudflared 的安装、卸载与上传；进度轮询挂在页面上，关闭弹窗后安装继续进行并照常给出终态提示
- **修改配对密码（Web 管理后台）**：Web 端支持修改设备配对密码
- **监控数据导出与清理（Web 管理后台）**：系统监控页支持导出全部指标 CSV、清理历史监控数据
- **仪表盘 WiFi 信息卡（Web 管理后台）**：仪表盘新增 WiFi 信息卡，信号与连接状态一览
- **隧道自动重连（Web 管理后台）**：隧道意外中断后按设定间隔自动重连，设备端服务重启也会恢复上次运行中的隧道；手动停止的隧道不会被自动拉起

#### 🔧 优化

- **通知配置与守护调度**：配置下发与守护状态由单一路径维护，不再有两套各自更新的入口
- **告警与短信计数不准**：此前一个最大只返回 1、一个硬编码 0，已补齐真实计数

- **设置页栅格改固定两列（Web 管理后台）**：消除宽屏下自动摊列导致的卡片高度参差
- **更新页与配对页改通栏（Web 管理后台）**：这两页卡片高度随运行时状态变化，通栏后不再出现底边参差与下方空洞
- **配色两套并存（Web 管理后台）**：修复同一页面里「原生按钮一个色、自研控件另一个色」的问题，全站组件与图表配色现在统一
- **测速界面重做（Web 管理后台）**：改为表盘式界面（指针 + 刻度 + 阶段进度），补上延迟 / 抖动与实时速率曲线；测速节点由设备侧转发，浏览器不必能直连外网
- **网络制式状态同步（Web 管理后台）**：切换期间显示「切换中」，不再把界面弹回切换前的档位

#### 🔒 安全加固

- **备份接口防滥用**：导出用的密钥派生迭代次数设上限（500 万），防止配对端传入极大值拖死服务；解压包设单条 4MB、累计 16MB 双上限，防解压炸弹；明文导出必须显式确认后才执行
- **签名口令不再明文**：签名口令从构建脚本移出到本机忽略文件，脚本零明文；持续集成增加「凭据不得入库」与「源码必须 UTF-8」两道检查
- **登录失败区分可重试与已失效**：可重试的临时失败不再抹掉配对关系；配对密码增加尝试次数限制

#### 🐛 修复

- **可自愈故障被当成凭据失效**：此前失败一律按「凭据已吊销」处理，让时钟漂移这类临时故障去销毁用户凭据，是「通知和连接莫名失效」的根因；现按三档区分
- **失败提示更准确**：设备侧的三种失败（参数越界 / 会话失效 / 设备拒绝）现在分别返回，客户端能给出可操作的处理提示
- **伪装来源提示**：修复局域网 / 本机操作被误判为公网来源、在内网也提示「远程连接」的问题
- **并发请求偶发失败**：修复缓存锁在「取得锁对象」与「真正上锁」之间的极短窗口内被误回收、导致两个请求同时进入临界区的问题

- **文件上传失败（Web 管理后台）**：上传请求的签名方式不正确（误按下载的方式签名），已改正
- **实时曲线笔尖跑偏（Web 管理后台）**：曲线动效重做为「画面匀速左移、笔尖固定在右边框」；横轴间距改为按实际到达间隔计算，此前推送间隔与预设不一致时，笔尖会每来一帧就回跳一次，间隔越短偏得越多

---

### 🧩 本轮合并（文件管理器 / 安装器 / 更新体验 / 问题修复）

> 以下为 v0.0.2 之后合并到 `main` 的迭代；版本号保持 `0.0.2`，更新内容一并归入本节。

#### 📱 App 端

- **文件管理器增强**：支持解压 / 压缩、文件校验和（Checksum）、复制路径，core / web / app 三端协同
- **手机版 Core 安装器**：内置 UFI-AXIS Core 安装器（Android），一键向设备推送并安装 Core；软件名改为「UFI-AXIS Core 安装器」，图标复用 Core 风格、徽章文字为 Install；UI 对齐 UFI-AXIS 默认中性皮肤（环形进度、语义色结果横幅、自适应日志面板）
- **更新弹窗体验**：Core / App 更新拆分为独立弹窗、Core 优先展示；进度统一为环形（UfiRingProgress 116dp + 中心百分比）
- **Core 更新前端重连续管**：App 退出后若 Core 仍在更新，冷启动自动接管进度显示并恢复轮询，终态清除标记
- **验证码自动复制与同步开关解耦**：撤销无障碍仅影响自动复制
- **控制台历史**：WS 重连后补拉历史，Web / App 两端一致
- **崩溃 dump 镜像到 Download**：与 App 同口径便于取回

#### 🖥 Core 端（管理后台，含 Web 前端）

- **文件管理器（服务端）**：解压 / 压缩 / 校验和接口
- **日志分类重写**：平铺日志文件收进分类子目录
- **修复 WebhookDelivery 占位符正则导致后端闪退**（下载触发）
- **发布流程**：release 说明改为 App / Core / Web 分类输出 + Markdown 下载表；下载表增加镜像下载列（mirror.drxian.qzz.io）
- **安装器接入 release**：手机版 Core 安装器融入并接入编译 / 重命名；PC 一键安装器产物改名 `UFI-AXIS-Core-install-Windows-Linux-<ver>.zip`

#### 🐛 修复（跨端）

- Webhook 后端闪退、部分接口「后端不支持」提示、自动复制验证码失效
